# 開発タスクリスト (Todo)

## フェーズ1: Docker環境構築と基本設定

- [x] `Dockerfile` を作成する
    - ベースイメージとして公式の `python:3.10-slim` を指定する
    - 作業ディレクトリを設定する
    - `requirements.txt` をコンテナにコピーし、ライブラリをインストールする
    - アプリケーションコードをコンテナにコピーする
    - スクリプトの実行コマンドを指定する
- [x] `docker-compose.yml` を作成する
    - `Dockerfile` をビルドしてサービスを定義する
    - `.env` ファイルをコンテナ内の環境変数として読み込む設定を行う
    - データ永続化のため、処理済みツイートIDファイルを保存するボリュームを設定する
- [x] `requirements.txt` ファイルを作成し、必要なライブラリをリストアップする
    - `feedparser`
    - `tweepy`
    - `google-generativeai`
    - `schedule`
    - `python-dotenv`
- [x] 設定ファイル `.env.example` を作成する（APIキーや設定値のテンプレート）
- [x] `.gitignore` ファイルを作成し、`.env` や `__pycache__` などをバージョン管理から除外する
- [x] `docker-compose build` コマンドでDockerイメージをビルドする

## フェーズ2: コア機能のモジュール開発

### 2.1. ツイート収集モジュール (`browser_fetcher.py`)
- [x] Seleniumを使用してブラウザを起動し、Xのサイトにアクセスする機能を実装する
- [x] ログイン処理を実装する
- [x] 指定された検索クエリでツイートを検索し、投稿内容を取得する関数を作成する
- [x] 取得した投稿から、本文、URL、IDを抽出する
- [x] ステルス設定を実装し、Bot検知を回避する試みを行う
- [x] 処理済みのツイートIDを記録するテキストファイル (`processed_ids.txt`) を読み書きする関数を作成する
- [x] 新しく取得したツイートが、処理済みIDリストに含まれていないかチェックする関数を作成する

### 2.2. AIコメント生成モジュール (`gemini_client.py`)
- [x] 設定ファイルからGemini APIキーを読み込む機能を実装する
- [x] `google-generativeai` ライブラリを初期化する
- [x] 取得したツイート本文を元に、引用コメント生成用のプロンプトを作成する関数を実装する
- [x] Gemini APIにリクエストを送信し、生成された日本語コメントを取得する関数を作成する

### 2.3. X投稿モジュール (`twitter_client.py`)
- [x] 設定ファイルからXのAPIキーとアクセストークンを読み込む機能を実装する
- [x] `tweepy` を使用して、X API (v2) のクライアントを認証・初期化する関数を作成する
- [x] AIが生成したコメントと元のツイートURLを組み合わせて、引用リツイートを投稿する関数を作成する

## フェーズ3: 全体の統合と自動化

### 3.1. メインスクリプト (`main.py`)
- [x] 各モジュール（`browser_fetcher`, `gemini_client`, `twitter_client`）をインポートする
- [x] 「ツイート収集 → コメント生成 → 投稿」の一連のワークフローを実行するメイン関数を実装する
- [x] Xへの接続エラー、APIエラーなど、各ステップでのエラーハンドリングを実装する（try-except）
- [x] 処理の状況やエラー情報をコンソールやログファイルに出力するロギング機能を実装する
- [x] 投稿が成功したら、処理済みツイートIDをファイルに追記する処理を実装する

### 3.2. スケジューリング
- [x] `schedule` ライブラリを使用して、メイン関数を一定時間ごとに（例: 1時間に1回）実行する処理を `main.py` に追加する
- [x] プログラムが終了しないように、無限ループでスケジューラを稼働させる処理を実装する

## フェーズ4: テストとドキュメント

- [ ] 各モジュールの単体テストを行う（可能であれば）
    - Nitterから正しくデータを取得できるか
    - Geminiが意図した形式のコメントを返すか
    - Xへの投稿が成功するか（テスト投稿）
- [ ] 実際に一連の流れを通して、システム全体が正しく動作するか結合テストを行う
- [ ] `README.md` を作成し、プログラムの概要、設定方法、実行方法を記述する
