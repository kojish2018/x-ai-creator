# AIニュース自動引用リツイートプログラム 要件定義書

## 1. 目的

海外（英語圏）で話題となっている革新的なAI関連のニュースやツイートを自動で収集し、AIが生成した日本語の解説や意見を付け加えて引用リツイートする。これにより、専門的で鮮度の高い情報を発信するXアカウントを半自動で運用し、情報収集・発信の効率を最大化することを目的とする。

## 2. 機能要件

### 2.1. ツイート収集機能
- **データソース:** 公開されているNitterインスタンスのRSSフィードを利用する。
- **検索条件:**
    - 検索キーワードを指定できること。（例: `"AI breakthrough"`, `"innovative AI"`, `"AGI"`など）
    - 検索対象を英語 (`lang:en`) のツイートに限定する。
    - 話題性を担保するため、一定数以上の「いいね」がついたツイートのみを対象とする (`min_faves`) 機能を持つこと。
- **重複投稿防止:**
    - 一度処理したツイートのIDを記録・管理し、同じツイートを繰り返し引用しない仕組みを持つこと。

### 2.2. 引用コメント生成機能
- **AIモデル:** Gemini API（本AI）を利用して、引用リツイートの文章を生成する。
- **インプット:** 収集したツイートの本文をAIへの入力とする。
- **アウトプット:**
    - 生成する文章は日本語とする。
    - 元ツイートの要約、それに対する意見、補足情報などを盛り込んだ、示唆に富むコメントを生成する。
    - 生成するコメントのトーン（例: 専門家風、カジュアル、中立など）をプログラム側で調整できることが望ましい。

### 2.3. 投稿機能
- **プラットフォーム:** X (旧Twitter)
- **投稿形式:** 引用リツイート
- **利用API:** Xの公式API（v2推奨）の無料プラン枠内で投稿処理を行う。
- **投稿内容:** 「2.2.で生成した日本語コメント」と「2.1.で収集した元のツイートのURL」を組み合わせて投稿する。

### 2.4. 自動実行機能
- 上記の「収集 → 生成 → 投稿」までの一連のプロセスを、指定したスケジュール（例: 1時間に1回、1日に3回など）で自動的に実行する機能を持つこと。

## 3. 非機能要件

### 3.1. コスト
- 全ての機能を、無料で利用可能なサービス・ライブラリ・APIプランの範囲内で実現すること。

### 3.2. 設定・構成
- 以下の項目を、外部の設定ファイル（例: `config.ini`, `.env`）で容易に変更・管理できるようにすること。
    - X APIキー (API Key, API Secret, Access Token, etc.)
    - 検索キーワード
    - いいねの最小フィルタ数 (`min_faves`)
    - 利用するNitterインスタンスのURL
    - 自動実行のスケジュール間隔
    - 処理済みツイートIDを保存するファイルパス

### 3.3. 耐障害性・安定性
- Nitterインスタンスへの接続失敗や、APIからのエラー応答など、予期せぬエラーが発生した場合でもプログラム全体が停止しないこと。
- エラーが発生した場合は、その内容をログファイルに出力し、次の実行サイクルまで待機するなどの適切なエラーハンドリングを行うこと。

### 3.4. 開発環境
- **言語:** Python 3.x
- **主要ライブラリ:**
    - **RSS解析:** `feedparser`
    - **X API連携:** `tweepy`
    - **Gemini API連携:** `google-generativeai`
    - **スケジューリング:** `schedule`
    - **設定ファイル管理:** `python-dotenv`

## 4. システム構成概要

```mermaid
graph TD
    A[スケジューラ] --> B{Pythonスクリプト};
    B -- 1. RSSフィード取得 --> C[Nitter];
    C -- 2. ツイート情報 --> B;
    B -- 3. 処理済みか確認 --> D[ツイートIDデータベース (テキストファイル)];
    B -- 4. コメント生成依頼 --> E[Gemini API];
    E -- 5. 生成されたコメント --> B;
    B -- 6. 引用リツイート投稿 --> F[X API];
    B -- 7. 処理済みIDを保存 --> D;
```
